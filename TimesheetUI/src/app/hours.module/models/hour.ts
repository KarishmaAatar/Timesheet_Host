import { Time } from '@angular/common';

export class Hour {
    AuditId: string;  
    UserId: string;  
    SessionId: string; 
    IPAddress: string;  
    LoginAt: Time;  
    LogoutAt: Time;  
    LoginStatus: string;  
} 